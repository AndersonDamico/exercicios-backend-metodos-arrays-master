const nomes = ['Ford Ká', 'Ranger', 'Hilux', 'Corola', 'Fusca', 'Chevete', 'Brasilia'];
const posicao = 3;

const grupo = (carros, posicao) => {
    console.log(carros.splice(position, 3). join(" - "));

}

grupos (nomes, posicao)